[project]
name = "media-organizer"
version = "0.1.0"
description = "Simple media file organizer using guessit + TMDb"
requires-python = ">=3.11"
dependencies = [
  "guessit>=3.6.0",
  "requests>=2.32.0",
]

[project.scripts]
organize-media = "organize_media:main"
